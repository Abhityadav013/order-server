{
  "version": 2,
  "builds": [
    { "src": "api/server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/v1/(.*)", "dest": "/api/server.ts" },
    { "src": "/webhook/(.*)", "dest": "/api/server.ts" },
    { "src": "/(.*)", "dest": "/api/server.ts" }
  ]
}
